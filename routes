const express = require('express');
let proyecto = require('../controllers/proyectoController.js');
//defino a un Router
let router = express.Router();

router.get('/', proyecto.list);

module.exports = router;
